# Business portal (web/ loads this file from the repository root).
# Local development uses Vite's backend proxy when this is blank.
VITE_API_BASE_URL=
VITE_TENANT_BASE_DOMAIN=localhost
VITE_ADMIN_URL=http://localhost:5174
# Production example: VITE_TENANT_BASE_DOMAIN=example.com
# Do not put server signing secrets or shared internal API keys in VITE_ variables.
