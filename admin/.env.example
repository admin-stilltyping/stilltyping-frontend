# Super-admin frontend. Copy to admin/.env if overriding local defaults.
VITE_API_BASE_URL=
# Apex URL of the business portal; the business slug is prepended as a subdomain.
VITE_WEB_URL=http://localhost:5173
# Production example: VITE_WEB_URL=https://example.com
