# Super-admin console

React + Vite + TanStack Query dashboard for platform operators: businesses,
plan defaults, feature requests, AI usage, request timing, audit log, and
the AI playbook. Authenticates against `POST /auth/super-admin/login` in
`stilltyping-backend`.

```bash
npm install
npm run dev -w admin
```
